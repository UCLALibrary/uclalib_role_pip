---

python_base_packages:
  - python{{ python_version }}
  - python{{ python_version }}-setuptools
  - python{{ python_version }}-devel
  - python{{ python_version }}-pip

python_symblink_src_path: "/bin/pip{{ python_version[0] }}.{{ python_version[1] }}"
